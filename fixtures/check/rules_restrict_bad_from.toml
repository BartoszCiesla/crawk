# A restrict rule whose `from` names a non-existent module → UnknownRuleModule
# (exit 2). A separate config from the bad-`to` case: validation stops at the
# first error, so one file cannot exercise both paths.

[[check.restrict]]
from = "clii"
to = ["web"]
